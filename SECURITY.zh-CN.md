# 安全问题

简体中文 | [English](SECURITY.md)

Token Killer 会处理 API Key 和 OAuth 凭据，因此请不要在公开 Issue 中粘贴任何真实凭据、授权码、请求日志或账号信息。

如果你发现了可能泄露凭据、绕过排行榜校验或影响其他用户的问题，请优先使用 GitHub 仓库的 **Report a vulnerability** 私密报告入口。报告里只需要提供最小复现；不要上传仍然有效的生产密钥。

普通功能错误和界面问题可以直接提交 Issue。

## 浏览器凭据边界

API Key 与 OAuth 凭据使用浏览器生成的不可导出 AES-GCM 密钥加密，并保存在 IndexedDB 中。这可以避免明文凭据被随手看到或混入设置导出，但不能抵御同源恶意脚本：XSS、被入侵的浏览器扩展或被篡改的部署版本仍可能在页面打开时使用这些凭据。建议使用独立域名、及时更新依赖，并且只部署经过审阅的构建产物。

普通 API 推理请求始终由静态前端直接发出。订阅 OAuth 因令牌交换和上游兼容需要可选授权 Worker；凭据与 Prompt 会在请求期间经过该 Worker 的内存，但不会写入排行榜数据库。

当推理请求的网络结果无法确认时，Token Killer 不会自动重试。超时、CORS 失败或连接中断可能发生在 Provider 已经接收并计费之后，因此本次运行会停止，并提示用户到 Provider 自身的用量记录中核对。

可选的地区探测服务会为地区排行榜签发短时网络出口凭证。凭证只包含规范化后的国家、省市、时间和随机 nonce，不包含安装编号、API 凭据、Prompt 或模型响应。探测服务在网络层必然能看到访问者源 IP；经过代理时，这个地址可能已经变成代理出口。运营者应关闭访问日志或设置合适的保留期限。只有可信反向代理已经清除访客伪造的地区 Header、再写入自己的识别结果时，才能启用 `TRUST_GEO_HEADERS`。同样，只有 Node 服务藏在可信代理之后、代理会覆盖客户端 IP Header 时，才能启用 `TRUST_PROXY_IP_HEADERS`，否则访客可以伪造地址参与 GeoIP 查询。
